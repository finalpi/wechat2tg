version: '3.9'

services:
  gewechat:
    image: xleat/gewe:latest
    container_name: gewechat
    ports:
      - "2531:2531"
      - "2532:2532"
    volumes:
      - ./temp:/root/temp
    restart: always
